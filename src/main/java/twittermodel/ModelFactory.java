package twittermodel;

import utils.OneToOneHash;

public class ModelFactory {
    private OneToOneHash<Integer, String> wikinameIdMap = new OneToOneHash<>();
    private OneToOneHash<Integer, String> interestIdMap = new OneToOneHash<>();


}
